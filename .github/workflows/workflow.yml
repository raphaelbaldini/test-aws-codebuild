name: Hello World

on:
  push:
  workflow_dispatch:

jobs:
  Hello-World-Job:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Build
        run: echo "Hello World"
